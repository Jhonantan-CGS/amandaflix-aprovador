rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /amandaflixApprovalRequests/{requestId} {
      allow read, create, update: if true;
      allow delete: if false;
    }
  }
}
